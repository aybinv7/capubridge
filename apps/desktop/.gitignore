# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Guard against a literal "~" directory ever being created/tracked here again
# (an earlier `git add -A` accidentally committed apps/desktop/~/.tauri/*.pub —
# a mis-expanded home-dir path from a signing command run in this directory).
/~/